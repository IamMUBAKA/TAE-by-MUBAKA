# MUBAKA FALSE SUCCESS BLOCK

Forbidden pattern:
`dependency failed -> variable unset -> later command runs -> success line prints`

If dependency, auth, path, command, or artifact verification fails, stop with:
`BLOCKED_REVIEW_REQUIRED`

No completion claims are valid without matching proof and receipt evidence.

Mandatory clause:
“O-O-MUBAKA is closed only when visual posture, mechanical execution, public/private boundary, receipt truth, native exit-code verification, artifact existence verification, and founder-control authority all pass together. No isolated success state is valid outside the full cadence.”
